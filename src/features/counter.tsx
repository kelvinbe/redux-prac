// import { createAction } from "@reduxjs/toolkit";


// const incremnent = createAction('INCREMENT')


// const incremnentAction = incremnent()

// incremnentAction.type



// export const reducer = (state: CounterState, action: CounterAction)
// if(action.type === incremnent.type) {

// }